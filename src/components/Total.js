export default function Total(props) {
    return (
        <div className='text-slate-200 text-end text-xl'>{props.value}</div>
    
    // null
    );
  }

  // const totalByPeople = bill / numberPeople + tipByPeople